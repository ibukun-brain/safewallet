from django.urls import path
from accounts import views as account_views

app_name = "account_wallet"

urlpatterns = [
    # path(
    #     "verify",
    #     view=account_views.CreateWalletView.as_view(),
    #     name="verify_user",
    # )
]